name: myfirstpipeline

on:
  workflow_dispatch:  #manual trigger
jobs:
  myfirstjob:
   name: first_job_name
   runs-on: ubuntu-latest # agent hai ye
   steps:
     - name: 
       run: echo "hello guys"
  mysecondjob:
   needs: myfirstjob
   name: second_job_name
   runs-on: windows-latest # agent hai ye
   steps:
     - name: 
       run: write-host "hello guys1"
